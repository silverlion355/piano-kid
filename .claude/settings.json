{
  "permissions": {
    "allow": [
      "Bash(git *)",
      "Bash(cc-connect *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *.js|*.json|*.yml|*.yaml|*.xml|*.html|*.css) if command -v prettier >/dev/null 2>&1; then prettier --write \"$f\"; fi ;; esac; }'",
            "timeout": 30,
            "statusMessage": "Formatting changed files"
          }
        ]
      }
    ]
  }
}
